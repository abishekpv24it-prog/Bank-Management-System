# Bank-Management-System
A menu-driven C program that simulates basic banking operations such as deposit, withdrawal, and balance inquiry using functions and a global balance variable. The program continuously displays the menu until the user chooses to exit.
